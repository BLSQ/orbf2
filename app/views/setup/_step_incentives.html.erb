<%if current_program.project &&  current_program.project.id%>
<% if step.model%>
  <%= link_to 'New Configuration', new_setup_project_incentive_path(current_program.project), class: 'btn btn-info pull-right' %>
  <table class="table table-striped table-condensed">
      <thead>
          <tr>
              <th>headers...</th>
          </tr>
      </thead>
      <body>
          <%= render partial: "incentive", collection: step.model %>
      </body>
  </table>
  <% else %>
    <%= link_to 'Let\'s configure incentives', new_setup_project_incentive_path(current_program.project)  %>
  <% end %>
<%end%>
