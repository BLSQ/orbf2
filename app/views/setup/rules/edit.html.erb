<%= simple_form_for rule, url: setup_project_package_rule_path(rule) do |f| %>
  <%= render partial: "form", locals: { f: f }%>
  <br><br>
  <%= f.button :submit, class: "btn btn-success" %>
<% end %>
