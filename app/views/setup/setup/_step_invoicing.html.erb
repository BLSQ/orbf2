
<% if project.id %>
  <%= link_to 'Configure dhis2 mapping of formulas', new_setup_project_formula_mapping_path(project), class: 'btn btn-info' %><br><br>
  <%= link_to 'Sync output datasets', new_setup_project_dataset_path(project), class: 'btn btn-info' %><br><br>
  <%= link_to 'Invoicing simulation', new_setup_project_invoice_path(project), class: 'btn btn-primary'  %><br><br>

<% end %>
